server {
    listen       80;
    listen       [::]:80;
    server_name  zhaolei.example.com;
    return 301   https://$host$request_uri;
}

server {
    listen       443 ssl;
    listen       [::]:443 ssl;
    listen       443 quic;
    listen       [::]:443 quic;
    server_name  zhaolei.example.com;

    ssl_certificate      /etc/nginx/certs/example.com_fullchain.pem;
    ssl_certificate_key  /etc/nginx/certs/example.com_key.pem;

    resolver 8.8.8.8 1.1.1.1 valid=30s ipv6=off;
    resolver_timeout 5s;

    client_max_body_size 1m;
    add_header_inherit merge; # 合并继承上级配置中的 add_header, 仅对 add_header 指令生效, 该指令出现在版本 1.29.3 中
    more_set_headers 'Alt-Svc: h3=":443"; ma=86400';

    # 访问控制
    # access_by_lua_file /etc/nginx/lua/access/entry.lua;

##############################
    # https://nginx.org/en/docs/http/ngx_http_autoindex_module.html
    location / {
        autoindex on; # 启用目录索引, 默认 off, 当目录中没有可用的索引文件时自动生成文件列表
        autoindex_exact_size off; # 控制目录索引中的文件大小是否显示精确字节数, 默认 on, 关闭后以 KB/MB/GB 等可读单位显示
        autoindex_format html;
        autoindex_localtime on; # 控制目录索引中的文件时间使用本地时区还是 UTC, 默认 off, 开启后使用服务器本地时区
    }

    location ~ (^|/)\. {
        return 444;
    }

    location = /favicon.ico {
        root /usr/local/openresty/nginx/html;
        access_log off;
        log_not_found off;
    }

    location = /robots.txt {
        root /usr/local/openresty/nginx/html;
        allow all;
        log_not_found off;
        access_log off;
    }

##############################
# internal html.
    error_page 403 /403.html;
    location = /403.html {
        root /usr/local/openresty/nginx/html;
        internal;
    }

    error_page 404 /404.html;
    location = /404.html {
        root /usr/local/openresty/nginx/html;
        internal;
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/local/openresty/nginx/html;
        internal;
    }
}
